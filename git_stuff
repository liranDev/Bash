# this is git config

git config --global user.name = "NAME
git config --global user.email = "NAME@gmail.com"

git config --global core.editor vim
git config --global help.autocorrect 1
git config --global color.ui auto
git config --global core.autocrlf true

# some git commands

git checkout file --> revert added changes
git config --global alias.lga "log --oneline --graph --all --decorate"
git branch --set-upstream master origin/master # tracks remote branch
git checkout -b NAME # create and checkout branch
